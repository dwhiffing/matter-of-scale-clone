describe('ProfileSummary', () => {
  it("should go to profile photo gallery when clicked");
  it("should go to currentState profile photo gallery when clicked and isCurrent is true");
  it("renders black background and a gallery when profile has photos to display");
  it("renders gray background and the default photo when profile has no photos");
  it("renders a favorite button when isCurrent is true");
  it("does not render a favorite button when isCurrent is false");
})
